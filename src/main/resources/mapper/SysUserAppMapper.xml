<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xin.cymall.dao.SysUserAppDao">

	<select id="queryList" resultType="xin.cymall.entity.SysUserApp">
		select * from sys_user_app 
		<where>
			<if test="userId != null">
				user_id = #{userId}
			</if>
		</where>
	</select>
 
	<insert id="save" keyProperty="id" useGeneratedKeys="false">
		insert into sys_user_app
		(
			id,
			user_id, 
			app_id
		)
		select seq_sys_user_app.NEXTVAL,A.* from(
		<foreach collection="appIdList" item="item" index="index" separator="union all">
			select
			#{userId},
			#{item}
			from dual
		</foreach>
		) A
	</insert>
	
	<delete id="delete">
		delete from sys_user_app where user_id = #{value}
	</delete>
	
	<select id="queryAppIdList" resultType="long">
		select app_id from sys_user_app where user_id = #{value}
	</select>
</mapper>