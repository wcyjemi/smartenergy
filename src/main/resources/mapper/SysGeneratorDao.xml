<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xin.cymall.dao.SysGeneratorDao">
	<select id="queryList" resultType="map">
		select tbb.* from (
		select rownum rn,tb.table_name tableName, tb.engine, tb.table_comment tableComment, tb.create_time createTime from user_tables tb 
			where tb.table_schema = (select database()) 
		<if test="tableName != null and tableName.trim() != ''">
			and tb.table_name like concat(concat('%', #{tableName}), '%') 
		</if>
		order by tb.create_time desc
		) tbb
		<where>
			<if test="offset != null and limit != null">
				tbb.rn between #{offset} and #{offend}
			</if>
		</where>
	</select>
	
	<select id="queryTotal" resultType="int">
		select count(*) from user_tables where table_schema = (select database())
		<if test="tableName != null and tableName.trim() != ''">
			and table_name like concat(concat('%', #{tableName}), '%') 
		</if> 
	</select> 
	
	<select id="queryTable" resultType="map">
		select table_name tableName, engine, table_comment tableComment, create_time createTime from user_tables
			where table_schema = (select database()) and table_name = #{tableName}
	</select> 
	
	<select id="queryColumns" resultType="map">
		select column_name columnName, data_type dataType,column_type  maxLength, column_comment columnComment, column_key columnKey, extra from user_tab_columns
 			where table_name = #{tableName} and table_schema = (select database()) order by ordinal_position
	</select>
</mapper>