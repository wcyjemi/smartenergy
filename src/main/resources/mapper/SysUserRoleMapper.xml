<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="xin.cymall.dao.SysUserRoleDao">

	<select id="queryList" resultType="xin.cymall.entity.SysUserRole">
		select * from sys_user_role 
		<where>
			<if test="userId != null">
				user_id = #{userId}
			</if>
		</where>
	</select>
 
	<insert id="save" keyProperty="id" useGeneratedKeys="false">
		insert into sys_user_role
		(
			id,
			user_id, 
			role_id
		)
		select seq_sys_user_role.NEXTVAL,A.* from(
		<foreach collection="roleIdList" item="item" index="index" separator="union all">
			select
			#{userId},
			#{item}
			from dual
		</foreach>
		) A
	</insert>
	
	<delete id="delete">
		delete from sys_user_role where user_id = #{value}
	</delete>
	
	<select id="queryRoleIdList" resultType="long">
		select role_id from sys_user_role where user_id = #{value}
	</select>
</mapper>